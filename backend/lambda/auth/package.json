{
  "name": "zentriqvision-auth-lambda",
  "version": "1.0.0",
  "description": "Authentication Lambda function for ZentriqVision",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "bundle": "esbuild dist/index.js --bundle --platform=node --target=node18 --external:aws-sdk --external:@aws-sdk/client-dynamodb --external:@aws-sdk/util-dynamodb --external:@aws-sdk/client-s3 --external:@aws-sdk/s3-request-presigner --outfile=dist/bundle.js",
    "build:lambda": "npm run build && npm run bundle",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.0.0",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.2.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/jsonwebtoken": "^9.0.0",
    "@types/node": "^18.0.0",
    "esbuild": "^0.25.8",
    "typescript": "^4.9.0"
  },
  "keywords": [
    "aws",
    "lambda",
    "cognito",
    "authentication"
  ],
  "author": "ZentriqVision",
  "license": "MIT"
}
