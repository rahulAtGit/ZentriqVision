{
  "name": "zentriqvision-playback-lambda",
  "version": "1.0.0",
  "description": "Video playback Lambda function for ZentriqVision",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "bundle": "esbuild dist/lambda/playback/index.js --bundle --platform=node --target=node18 --external:aws-sdk --external:@aws-sdk/client-dynamodb --external:@aws-sdk/util-dynamodb --external:@aws-sdk/client-s3 --external:@aws-sdk/s3-request-presigner --outfile=dist/bundle.js",
    "build:lambda": "npm run build && npm run bundle",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/s3-request-presigner": "^3.0.0",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.2.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/jwks-rsa": "^3.0.0",
    "@types/node": "^18.0.0",
    "@types/uuid": "^9.0.0",
    "esbuild": "^0.25.8",
    "typescript": "^4.9.0"
  },
  "keywords": [
    "aws",
    "lambda",
    "video",
    "playback",
    "s3"
  ],
  "author": "ZentriqVision",
  "license": "MIT"
}
